# GitHub Actions 构建准备

请将同目录的 `拼豆图纸工作台_v0.21.0_macos_dmg_source.zip` 上传到本仓库根目录，文件名不要改。

上传后进入 GitHub 的 **Actions**，选择 **Build macOS DMG**，点击 **Run workflow**。完成后在该运行页面的 Artifacts 下载 DMG。

压缩包不包含 `.data`、`.env`、API Key 或任何用户项目数据。
